{
  "name": "3d-space-invaders",
  "version": "1.0.8",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package": "npm version patch --no-git-tag-version && vite build && cd dist && zip -r ../3d-space-invaders.zip . && cd .. && echo 'Ready: 3d-space-invaders.zip'"
  },
  "dependencies": {
    "three": "^0.158.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-obfuscator": "^1.0.5"
  }
}
